version: '3'
volumes:
  db:
services:
  mysql:
    image: mysql:8.0
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: "usmp"
      MYSQL_USER: "usmp"
      MYSQL_PASSWORD: "usmp"
      MYSQL_ROOT_PASSWORD: "usmp"